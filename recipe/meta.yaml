{% set name = "awacs" %}
{% set version = "0.9.6" %}

package:
  name: "{{ name|lower }}"
  version: "{{ version }}"

source:
  url: "https://github.com/cloudtools/{{ name }}/archive/{{ version }}.tar.gz"
  sha256: "52681404f2731e93473a436b43375a30562f17a3bfc0064f986c2fb2aa73102a"

build:
  #TODO: Make noarch when python2 is dropped
  number: 0
  script: "{{ PYTHON }} -m pip install . --no-deps --ignore-installed -vv "

requirements:
  host:
    - pip
    - python
  run:
    - python

test:
  imports:
    - awacs
    - awacs.helpers
    - tests

about:
  home: "https://github.com/cloudtools/awacs"
  license: "BSD-2-Clause"
  license_family: "BSD"
  license_file: "LICENSE"
  summary: "AWS Access Policy Language creation library"
  dev_url: "https://github.com/cloudtools/awacs"

extra:
  recipe-maintainers:
    - agcopenhaver
